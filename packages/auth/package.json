{
  "name": "@hha/auth",
  "version": "0.1.0",
  "private": true,
  "description": "Server-side authentication primitives — JWT, password hashing, MFA, permission resolution.",
  "license": "UNLICENSED",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./jwt": "./src/jwt.ts",
    "./password": "./src/password.ts",
    "./permissions": "./src/permissions.ts",
    "./totp": "./src/totp.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint src --ext .ts",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@hha/shared": "workspace:*",
    "argon2": "^0.41.1",
    "jose": "^5.9.3"
  },
  "devDependencies": {
    "@hha/config": "workspace:*",
    "@types/node": "^20.16.10",
    "typescript": "^5.6.2",
    "vitest": "^2.1.1"
  }
}
